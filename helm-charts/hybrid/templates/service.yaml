apiVersion: v1
kind: Service
metadata:
  name: {{ include "hybrid.fullname" . }}
  labels:
    {{- include "hybrid.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hybrid.selectorLabels" . | nindent 4 }}
