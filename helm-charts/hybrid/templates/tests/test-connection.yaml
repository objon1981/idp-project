apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hybrid.fullname" . }}-test-connection"
  labels:
    {{- include "hybrid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hybrid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
